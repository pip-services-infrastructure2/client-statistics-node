export declare class StatCounterIncrementV1 {
    constructor(group: string, name: string, time: Date, timezone: string, value: number);
    group: string;
    name: string;
    time?: Date;
    timezone?: string;
    value: number;
}
